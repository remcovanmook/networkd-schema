# /etc/systemd/network/simple-bridge.network
# Minimum Version: systemd v211
# BRIDGING: SIMPLE BRIDGE (NETWORK)
#
# Configures the bridge interface br0 with a static IP.

[Match]
Name=br0

[Network]
Address=10.0.0.1/24
